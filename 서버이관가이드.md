# 설비 모니터링 서버 이관 가이드

## 1. 현재 서버 구성

- **Node.js 버전**: v24.14.0 이상
- **포트**: HTTP 3000 (HTTPS 리다이렉트 전용), HTTPS 3443 (실제 서비스)
- **프로세스 관리**: PM2 + pm2-windows-startup (Windows 부팅 시 자동 시작)
- **프로젝트 경로**: `C:\Users\jypark\equipment-monitor`

---

## 2. 복사할 파일 목록

### 서버 코드
| 파일 | 설명 |
|------|------|
| `server.js` | 메인 서버 (Express + Socket.io) |
| `graph.js` | Microsoft Graph API (Outlook 메일 연동) |
| `import-from-outlook.js` | 메일 가져오기 스크립트 |
| `package.json` | npm 의존성 목록 |
| `package-lock.json` | 의존성 버전 고정 |
| `ecosystem.config.js` | PM2 설정 (**cwd 경로 수정 필요**) |

### 웹 화면 (public/ 폴더 전체)
| 파일 | 설명 |
|------|------|
| `public/index.html` | 로그인/회원가입 + 설비 모니터링 페이지 |
| `public/vhwkd.html` | 출하 Daily 페이지 |
| `public/manifest.json` | PWA 설정 |
| `public/icon-192.svg` | 앱 아이콘 |

### 데이터 파일
| 파일 | 설명 |
|------|------|
| `data.json` | 장비/출하 상태 데이터 |
| `users.json` | 사용자 계정 (ID, 비밀번호, 팀) |
| `log.json` | 활동 로그 |
| `notifications.json` | FQC 알림 데이터 |

### 인증/설정 파일
| 파일 | 설명 |
|------|------|
| `key.pem` | SSL 개인키 (자체서명, 그대로 사용 가능) |
| `cert.pem` | SSL 인증서 (자체서명, 그대로 사용 가능) |
| `graph-config.json` | Microsoft Graph API 클라이언트 설정 (clientId, tenantId, clientSecret) |
| `graph-tokens.json` | Outlook 접근 토큰 (**만료 시 새 서버에서 재로그인 필요**) |
| `graph-state.json` | 마지막 메일 체크 시간 |

### 복사 불필요
- `node_modules/` — `npm install`로 자동 설치됨

---

## 3. 새 서버 설치 순서

```
1. Node.js 설치 (https://nodejs.org, v20 LTS 이상)
2. 위 파일들을 새 서버의 원하는 폴더에 복사
3. ecosystem.config.js 열어서 cwd 경로를 새 폴더 경로로 수정
4. 터미널(CMD 또는 PowerShell)에서:
   cd [새 폴더 경로]
   npm install
   npm install -g pm2
   pm2 start ecosystem.config.js
   pm2 save
5. Windows 자동 시작 설정:
   npm install -g pm2-windows-startup
   pm2-startup install
   pm2 save
6. Outlook 연동 확인:
   - 관리자 페이지(/rhksflwk) → Graph API 상태 확인
   - 토큰 만료 시 "Microsoft 로그인" 버튼으로 재인증
```

---

## 4. 이관 후 확인사항

- [ ] `https://[새서버IP]:3443` 접속 가능한지 확인
- [ ] `http://[새서버IP]:3000` 접속 시 HTTPS로 자동 리다이렉트 되는지 확인
- [ ] 로그인/회원가입 정상 동작
- [ ] 출하 Daily 페이지 데이터 표시 확인
- [ ] Outlook 메일 자동 체크 (매일 오전 7:30) 동작 확인
- [ ] 관리자 페이지 접속 가능한지 확인
- [ ] 핸드폰에서 접속 가능한지 확인 (같은 WiFi 필요)

---

## 5. 주의사항

- **방화벽**: 새 서버에서 3443 포트가 열려 있어야 함
- **IP 변경**: 서버 IP가 바뀌면 사용자들에게 새 접속 주소 안내 필요
- **DNS**: IT팀에 DNS 등록 요청 시 새 서버 IP로 변경
- **SSL 인증서**: 자체서명 인증서라 "안전하지 않음" 경고 뜸 → "계속 진행" 클릭하면 됨
- **graph-tokens.json**: Microsoft 토큰 유효기간이 있어서 이관 후 재로그인이 필요할 수 있음
